#include "main.h"

/**
 * int _sqrt_recursion - natural square root of a number
 * @n: int argument
 * Return: int
 */

int _sqrt_recursion(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	return (n, 0));
}
